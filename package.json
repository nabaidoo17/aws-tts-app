{
  "name": "aws-tts-app",
  "version": "1.0.0",
  "description": "AWS Text-to-Speech application",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "backend": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "install-deps": "cd frontend && npm install && cd ../backend && npm install",
    "deploy": "cd infrastructure && terraform apply"
  },
  "keywords": ["aws", "polly", "text-to-speech", "react"],
  "author": "",
  "license": "MIT"
}